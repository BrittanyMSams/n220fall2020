let reSize = document.getElementById("big")

reSize.style.backgroundColor = "#00FF00"
reSize.style.width = "100px";
reSize.style.height = "100px";

function Bigger(){
    let width = reSize.style.width
    let height = reSize.style.height

    if(height = onclick){
        reSize.style.height + "10px";
    }
    
    if(width = onclick){
        reSize.style.width + "10px";
    }
}
